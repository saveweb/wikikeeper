# WikiKeeper Environment Configuration

# Application
APP_NAME=WikiKeeper
APP_VERSION=0.1.0
DEBUG=false

# Server
HOST=0.0.0.0
PORT=8000

# Database (PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_USER=wikikeeper
DB_PASSWORD=wikikeeper123
DB_NAME=wikikeeper

# Admin Token (leave empty to disable admin authentication)
ADMIN_TOKEN=

# Logging
LOG_LEVEL=INFO

# HTTP Client
HTTP_TIMEOUT=30.0
HTTP_MAX_REDIRECTS=5
HTTP_USER_AGENT=WikiKeeper/0.1.0 (https://wikikeeper.saveweb.org/)

# Collection Settings
COLLECT_DELAY=1.5
COLLECT_BATCH_SIZE=50
COLLECT_RETRY_ATTEMPTS=3
COLLECT_RETRY_DELAY=5.0

# Archive.org
ARCHIVE_CHECK_DELAY=0.5
